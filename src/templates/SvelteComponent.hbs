
<script lang="ts">
	import type { ClassValue } from 'svelte/elements';
	interface Props {
		size?: number,
		class?: ClassValue,
		hidden?: boolean
	}
	let { size=24, hidden=false, ...props }: Props = $props();
	let px = $derived(`${size}px`);
</script>

<div style:height={px} style:width={px} class={props.class || ""} {hidden}>
	{{{ svgContent }}}
</div>
